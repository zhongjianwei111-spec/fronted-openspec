# TypeScript 规范

- 新代码使用项目已启用的严格程度，不通过扩大 `any`、`@ts-ignore` 或关闭规则绕过错误。
- 公共函数、组件输入、接口响应和状态边界声明明确类型；局部变量依赖可靠推断。
- 外部输入先按 `unknown` 处理，并在运行时校验或缩小类型。
- 使用判别联合表达互斥状态，避免多个布尔值组合出无效状态。
- 区分缺失值、空值和零值。不要用真值判断替代业务语义判断。
- 复用领域类型，避免为同一数据结构复制相似接口。
- 类型断言只用于代码已经验证、但类型系统无法推导的边界，并附带简短原因。
