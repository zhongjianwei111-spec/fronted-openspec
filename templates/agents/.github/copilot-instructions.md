# GitHub Copilot Instructions

Follow `AGENTS.md` and treat `.agents/rules/` as the canonical engineering rules.

Read `.agents/rules/00-index.md`, `.agents/rules/90-project-overrides.md`, the relevant task rules, and `.agents/rules/10-{{FRAMEWORK}}.md` before editing. Use the OpenSpec workflow boundary defined by `00-index.md`; localized fixes that restore established behavior can proceed directly.

Do not duplicate detailed engineering rules in this file.
