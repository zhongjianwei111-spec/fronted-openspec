# GitHub Copilot Instructions

Follow `AGENTS.md` and treat `.agents/rules/` as the canonical engineering rules.

Read `.agents/rules/00-index.md`, `.agents/rules/90-project-overrides.md`, the relevant task rules, and `.agents/rules/10-{{FRAMEWORK}}.md` before editing. Use the OpenSpec workflow for behavior or public-contract changes.

Do not duplicate detailed engineering rules in this file.
