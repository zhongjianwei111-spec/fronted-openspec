---
description: Shared frontend engineering and OpenSpec entry point
alwaysApply: true
---

# Shared Frontend Rules

Read `AGENTS.md` first. Treat `.agents/rules/` as the canonical source.

Before editing, read `.agents/rules/00-index.md`, `.agents/rules/90-project-overrides.md`, the task-specific rules, and `.agents/rules/10-{{FRAMEWORK}}.md`.

Use the OpenSpec workflow boundary defined by `00-index.md`; localized fixes that restore established behavior can proceed directly. Do not duplicate the detailed rules in Cursor-specific files.
